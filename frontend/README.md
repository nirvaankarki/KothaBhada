Frontend React app for KothaBhada

Commands:

Install dependencies:

```
cd frontend
npm install
```

Start dev server:

```
npm start
```

Notes:
- Backend must be running at `http://localhost:5001`.
- Endpoints used: `/api/auth/*` for auth flows.
